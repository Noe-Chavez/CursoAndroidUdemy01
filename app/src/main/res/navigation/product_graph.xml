<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/product_graph"
    app:startDestination="@id/fragmentCard">

    <fragment
        android:id="@+id/fragmentCard"
        android:name="mx.com.disoftware.FragmentCard"
        android:label="fragment_card"
        tools:layout="@layout/fragment_card" />
    <fragment
        android:id="@+id/productFragment"
        android:name="mx.com.disoftware.ProductFragment"
        android:label="fragment_product"
        tools:layout="@layout/fragment_product">
        <deepLink
            android:id="@+id/deepLink"
            app:uri="deeplinkejem://card" />
    </fragment>
</navigation>