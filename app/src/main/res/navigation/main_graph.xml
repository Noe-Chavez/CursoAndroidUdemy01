<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_graph"
    app:startDestination="@id/firstFragment">

    <fragment
        android:id="@+id/firstFragment"
        android:name="mx.com.disoftware.FirstFragment"
        android:label="fragment_first"
        tools:layout="@layout/fragment_first" >
        <action
            android:id="@+id/action_firstFragment_to_secondFragment"
            app:destination="@id/secondFragment"
            app:enterAnim="@anim/zoom_forward_in"/>
    </fragment>
    <fragment
        android:id="@+id/secondFragment"
        android:name="mx.com.disoftware.SecondFragment"
        android:label="fragment_second"
        tools:layout="@layout/fragment_second">
        <argument
            android:name="nombre"
            app:argType="string"
            app:nullable="true"
            android:defaultValue='"not name"' />
        <argument
            android:name="edad"
            app:argType="integer"
            android:defaultValue="-1" />
        <action
            android:id="@+id/action_secondFragment_to_product_graph"
            app:destination="@id/product_graph" />
    </fragment>
    <include app:graph="@navigation/product_graph" />
</navigation>